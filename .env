# Desarrollo local con Metro — la app en el dispositivo usa esta URL
EXPO_PUBLIC_API_URL=http://192.168.1.3:3000
# Para builds standalone (preview/production), la URL se define en eas.json

# Google Maps SDK API Key (Required for react-native-maps rendering engine)
# Note: This is ONLY for the map rendering SDK, NOT for map data
# We still use OpenStreetMap + Nominatim (Geocoding) + OSRM (Routing) for all map data
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=AIzaSyDu-vsndSIMluuvLfmGf_sAhQiNDliznrU

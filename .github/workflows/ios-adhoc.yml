name: iOS Ad Hoc Build

on:
  workflow_dispatch:

jobs:
  build:
    name: Build iOS Ad Hoc IPA
    runs-on: macos-latest
    timeout-minutes: 90

    env:
      NODE_VERSION: '20'
      IOS_PROJECT_PATH: 'ios'
      SCHEME: 'UrbanTaxiD'
      BUNDLE_ID: 'com.urbantaxi.app'
      TEAM_ID: 'FG4FGTT4Z4'
      P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
      KEYCHAIN_PASSWORD: 'keychain-pass-urbantax-2024'
      CERT_IDENTITY: 'iPhone Distribution: Maria velazquez motta (FG4FGTT4Z4)'
      PROFILE_NAME: 'UrbanTaxi SJ Ad Hoc'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Expo CLI
        run: npm install -g expo-cli eas-cli

      - name: Decode GoogleService-Info.plist (production)
        env:
          GOOGLE_SERVICE_INFO_PLIST_BASE64: ${{ secrets.GOOGLE_SERVICE_INFO_PLIST_BASE64 }}
        run: |
          node -e "require('fs').writeFileSync('GoogleService-Info.plist', Buffer.from(process.env.GOOGLE_SERVICE_INFO_PLIST_BASE64, 'base64'))"
          ls -la GoogleService-Info.plist

      - name: Decode GoogleService-Info-preview.plist (preview)
        env:
          GOOGLE_SERVICE_INFO_PREVIEW_PLIST_BASE64: ${{ secrets.GOOGLE_SERVICE_INFO_PREVIEW_PLIST_BASE64 }}
        run: |
          node -e "require('fs').writeFileSync('GoogleService-Info-preview.plist', Buffer.from(process.env.GOOGLE_SERVICE_INFO_PREVIEW_PLIST_BASE64, 'base64'))"
          ls -la GoogleService-Info-preview.plist

      - name: Prebuild iOS project
        run: |
          npx expo prebuild --clean --platform ios

      - name: Patch '@import' in Google-Maps-iOS-Utils headers
        run: |
          set -euo pipefail
          replaced=0
          for f in $(grep -rIl "@import" ${{ env.IOS_PROJECT_PATH }}/Pods/Google-Maps-iOS-Utils/Sources 2>/dev/null || true); do
            sed -i '' 's/@import \([A-Za-z0-9_]*\);/#import <\1\/\1.h>/' "$f"
            replaced=$((replaced+1))
          done
          for f in $(grep -rIl "@import" ${{ env.IOS_PROJECT_PATH }}/Pods/GoogleMaps/Maps/Sources 2>/dev/null || true); do
            sed -i '' 's/@import \([A-Za-z0-9_]*\);/#import <\1\/\1.h>/' "$f"
            replaced=$((replaced+1))
          done
          echo "Patched $replaced files"
          grep -rn "@import" ${{ env.IOS_PROJECT_PATH }}/Pods/Google-Maps-iOS-Utils/Sources 2>/dev/null || echo "No '@import' remaining in Google-Maps-iOS-Utils"

      - name: Configure manual code signing on app target
        run: |
          node -e "
            const fs = require('fs');
            const p = '${{ env.IOS_PROJECT_PATH }}/UrbanTaxiD.xcodeproj/project.pbxproj';
            let s = fs.readFileSync(p, 'utf8');
            const anchor = '\t\t\t\tDEVELOPMENT_TEAM = ${{ env.TEAM_ID }};';
            const inject = anchor + '\n' +
              '\t\t\t\tCODE_SIGN_STYLE = Manual;\n' +
              '\t\t\t\tCODE_SIGN_IDENTITY = \"iPhone Distribution\";\n' +
              '\t\t\t\tPROVISIONING_PROFILE_SPECIFIER = \"${{ env.PROFILE_NAME }}\";';
            const count = (s.split(anchor).length - 1);
            if (count < 2) {
              console.error('ERROR: esperaba al menos 2 ocurrencias de DEVELOPMENT_TEAM en el app target, encontradas:', count);
              process.exit(1);
            }
            s = s.split(anchor).join(inject);
            fs.writeFileSync(p, s);
            console.log('OK: firma manual (iPhone Distribution / Ad Hoc) inyectada en', count, 'build configs del app target (UrbanTaxiD)');
          "
          grep -n "CODE_SIGN_STYLE\|PROVISIONING_PROFILE_SPECIFIER\|CODE_SIGN_IDENTITY" ${{ env.IOS_PROJECT_PATH }}/UrbanTaxiD.xcodeproj/project.pbxproj

      - name: Create certs directory
        run: |
          mkdir -p ${{ env.IOS_PROJECT_PATH }}/certs
          ls -la ${{ env.IOS_PROJECT_PATH }}/certs/

      - name: Decode and install P12 certificate (Distribution)
        env:
          ADHOC_P12_BASE64: ${{ secrets.ADHOC_P12_BASE64 }}
          P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
        run: |
          set -euo pipefail
          echo "ADHOC_P12_BASE64 length (raw): ${#ADHOC_P12_BASE64}"
          node -e "
            const raw = process.env.ADHOC_P12_BASE64.replace(/\s+/g, '');
            const buf = Buffer.from(raw, 'base64');
            const fs = require('fs');
            fs.writeFileSync('${{ env.IOS_PROJECT_PATH }}/certs/dist-cert.p12', buf);
            console.log('ADHOC_P12_BASE64 length (sanitized):', raw.length, '(expected 4476)');
            console.log('Decoded bytes:', buf.length, '(expected 3356)');
            if (buf.length !== 3356) {
              console.error('ERROR: P12 distribuccion decodificado corrupto (longitud != 3356).');
              console.error('  Regenere con: base64 -i ios/certs/dist-cert.p12 | tr -d newline');
              process.exit(1);
            }
          "
          echo "--- Validacion openssl del P12 decodificado ---"
          if openssl pkcs12 -in ${{ env.IOS_PROJECT_PATH }}/certs/dist-cert.p12 -passin pass:"${P12_PASSWORD}" -nokeys -noout 2> /tmp/openssl_err.txt; then
            echo "OK: P12 abierto con P12_PASSWORD (MAC valida)"
            echo "--- Identidad del certificado ---"
            openssl pkcs12 -in ${{ env.IOS_PROJECT_PATH }}/certs/dist-cert.p12 -passin pass:"${P12_PASSWORD}" -clcerts -nokeys 2>/dev/null | openssl x509 -noout -subject
          else
            echo "FALLO: no se puede abrir el P12 con la P12_PASSWORD del secret."
            cat /tmp/openssl_err.txt
            exit 1
          fi
          echo "--- Normalizacion del P12 con algoritmos compatibles macOS (SHA1/3DES) ---"
          openssl pkcs12 -in ${{ env.IOS_PROJECT_PATH }}/certs/dist-cert.p12 \
            -passin pass:"${P12_PASSWORD}" -nodes -out /tmp/dist_extracted.pem
          openssl pkcs12 -export -in /tmp/dist_extracted.pem \
            -passout pass:"${P12_PASSWORD}" \
            -name "${{ env.CERT_IDENTITY }}" \
            -keypbe PBE-SHA1-3DES \
            -certpbe PBE-SHA1-3DES \
            -macalg sha1 \
            -out ${{ env.IOS_PROJECT_PATH }}/certs/dist_cert_normalized.p12
          openssl pkcs12 -in ${{ env.IOS_PROJECT_PATH }}/certs/dist_cert_normalized.p12 \
            -passin pass:"${P12_PASSWORD}" -nokeys -noout
          echo "OK: P12 distribuccion normalizado generado (MAC SHA1 + 3DES)"
          ls -la ${{ env.IOS_PROJECT_PATH }}/certs/

      - name: Decode and install Provisioning Profile (Ad Hoc)
        env:
          ADHOC_PROFILE_BASE64: ${{ secrets.ADHOC_PROFILE_BASE64 }}
        run: |
          echo "ADHOC_PROFILE_BASE64 length: ${#ADHOC_PROFILE_BASE64}"
          node -e "
            const raw = process.env.ADHOC_PROFILE_BASE64.replace(/\s+/g, '');
            const buf = Buffer.from(raw, 'base64');
            const fs = require('fs');
            fs.writeFileSync('${{ env.IOS_PROJECT_PATH }}/certs/profile-adhoc.mobileprovision', buf);
            console.log('ADHOC_PROFILE_BASE64 length (sanitized):', raw.length, '(expected 16848)');
            console.log('Decoded bytes:', buf.length, '(expected 12634)');
            if (buf.length !== 12634) {
              console.error('ERROR: Provisioning profile Ad Hoc decodificado corrupto (longitud != 12634).');
              process.exit(1);
            }
          "
          file ${{ env.IOS_PROJECT_PATH }}/certs/profile-adhoc.mobileprovision
          ls -la ${{ env.IOS_PROJECT_PATH }}/certs/

      - name: Create temporary keychain
        run: |
          echo "P12_PASSWORD from secret, length: ${#P12_PASSWORD}"
          security create-keychain -p "${KEYCHAIN_PASSWORD}" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "${KEYCHAIN_PASSWORD}" build.keychain
          security set-keychain-settings -t 3600 -u build.keychain

      - name: Import P12 certificate to keychain
        run: |
          echo "Importing normalized P12 (Distribution)..."
          security import ${{ env.IOS_PROJECT_PATH }}/certs/dist_cert_normalized.p12 \
            -k build.keychain \
            -P "${P12_PASSWORD}" \
            -T /usr/bin/codesign \
            -T /usr/bin/security
          echo "--- Certificados en keychain ---"
          security find-certificate -c "iPhone Distribution" build.keychain 2>&1 | head -5 || echo "(revisar identidad)"
          security find-identity -v -p codesigning build.keychain

      - name: Install Provisioning Profile (Ad Hoc)
        run: |
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp ${{ env.IOS_PROJECT_PATH }}/certs/profile-adhoc.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/
          echo "--- Perfil instalado (verificacion) ---"
          security cms -D -i ~/Library/MobileDevice/Provisioning\ Profiles/profile-adhoc.mobileprovision 2>/dev/null | grep -E "<key>(Name|UUID|ExpirationDate)" -A1 || true

      - name: Set keychain as default for codesign
        run: |
          security set-key-partition-list -S apple-tool:,apple: -s -k "${KEYCHAIN_PASSWORD}" build.keychain

      - name: Archive iOS App
        run: |
          cd ${{ env.IOS_PROJECT_PATH }}
          xcodebuild -workspace UrbanTaxiD.xcworkspace \
            -scheme ${{ env.SCHEME }} \
            -configuration Debug \
            -archivePath build/UrbanTaxiD.xcarchive \
            archive

      - name: Export IPA
        run: |
          cd ${{ env.IOS_PROJECT_PATH }}
          xcodebuild -exportArchive \
            -archivePath build/UrbanTaxiD.xcarchive \
            -exportPath build/ipa \
            -exportOptionsPlist ../export-options-adhoc.plist

      - name: Upload IPA as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: UrbanTaxiAdHoc-${{ github.run_number }}.ipa
          path: ${{ env.IOS_PROJECT_PATH }}/build/ipa/*.ipa
          retention-days: 30

      - name: Cleanup keychain
        if: always()
        run: |
          security delete-keychain build.keychain || true

      - name: Summary
        run: |
          echo "## Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "- **Build Number**: ${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Scheme**: ${{ env.SCHEME }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Configuration**: Debug" >> $GITHUB_STEP_SUMMARY
          echo "- **Bundle ID**: ${{ env.BUNDLE_ID }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Team ID**: ${{ env.TEAM_ID }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Signing**: iPhone Distribution (Ad Hoc)" >> $GITHUB_STEP_SUMMARY
          echo "- **IPA**: Available as artifact (Ad Hoc - no App Store)" >> $GITHUB_STEP_SUMMARY
          echo "- **Install via**: Diawi, direct install, or third-party store" >> $GITHUB_STEP_SUMMARY